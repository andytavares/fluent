// identity returns value unchanged.
export function identity<T>(value: T): T {
  // TODO
  return value;
}

// first returns the first element of arr, or undefined if arr is empty.
export function first<T>(arr: T[]): T | undefined {
  // TODO
  return undefined;
}

// zip pairs each element of as with the corresponding element of bs.
export function zip<A, B>(as: A[], bs: B[]): [A, B][] {
  // TODO
  return [];
}
