// TODO: define sealed class Expr with subclasses Num, Add, Mul, Neg

// evaluate recursively evaluates an Expr tree.
fun evaluate(expr: Expr): Double {
    // TODO
    return 0.0
}

// describe returns a human-readable string for an Expr tree.
fun describe(expr: Expr): String {
    // TODO
    return ""
}

fun main() {
    // val e = Add(Num(1.0), Mul(Num(2.0), Num(3.0)))
    // println(evaluate(e))   // 7.0
    // println(describe(e))   // (1.0 + (2.0 * 3.0))
}
