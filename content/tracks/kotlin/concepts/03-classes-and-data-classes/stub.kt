// TODO: define data class Money with amount: Double and currency: String

// TODO: extension function Money.add(other: Money): Money
// Throws IllegalArgumentException if currencies differ.

// TODO: extension function Money.format(): String
// Returns e.g. "USD 42.50"

// TODO: object ExchangeRates with:
//   fun usdToEur(amount: Double): Double  (multiply by 0.92)
//   fun eurToUsd(amount: Double): Double  (multiply by 1.0 / 0.92)

fun main() {
    // val m = Money(10.0, "USD")
    // println(m.format())          // USD 10.00
    // println(ExchangeRates.usdToEur(100.0))
}
