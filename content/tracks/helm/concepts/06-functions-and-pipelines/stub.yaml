apiVersion: v1
kind: Secret
metadata:
  # TODO: name = printf "%s-db-credentials" .Release.Name | trunc 63 | trimSuffix "-"
  name:
  # TODO: namespace = .Release.Namespace
  namespace:
  labels:
    # TODO: environment label = .Values.global.env | default "production" | lower
    environment:
type: Opaque
data:
  # TODO: DB_HOST = required "db.host is required" .Values.db.host | b64enc
  DB_HOST:
  # TODO: DB_PASSWORD = required "db.password is required" .Values.db.password | b64enc
  DB_PASSWORD:
  # TODO: DB_NAME = .Values.db.name | default "appdb" | b64enc | quote
  DB_NAME:
