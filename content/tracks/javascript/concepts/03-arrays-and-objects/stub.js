// pluck returns an array of obj[key] for each obj in arr.
function pluck(arr, key) {
  // TODO
  return [];
}

// groupBy groups arr into an object keyed by obj[key] for each obj.
function groupBy(arr, key) {
  // TODO
  return {};
}

// flatten flattens one level of nesting: [[1,2],[3,4]] -> [1,2,3,4].
function flatten(arr) {
  // TODO
  return [];
}

module.exports = { pluck, groupBy, flatten };
