// typeLabel returns typeof value as a string.
function typeLabel(value) {
  // TODO
  return "";
}

// strictEquals returns true if a === b.
function strictEquals(a, b) {
  // TODO
  return false;
}

// clamp returns value clamped to [min, max].
function clamp(value, min, max) {
  // TODO
  return 0;
}

module.exports = { typeLabel, strictEquals, clamp };
