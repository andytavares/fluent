// makeCounter returns a function that increments and returns a count.
// The count starts at `start` (default 0).
function makeCounter(start = 0) {
  // TODO
  return () => 0;
}

// makeAdder returns a function that adds n to its argument.
function makeAdder(n) {
  // TODO
  return (x) => x;
}

// once returns a wrapper that calls fn exactly once and memoizes the result.
function once(fn) {
  // TODO
  return fn;
}

module.exports = { makeCounter, makeAdder, once };
