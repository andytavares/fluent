// multiply returns a * b. b defaults to 1.
function multiply(a, b = 1) {
  // TODO
  return 0;
}

// sum returns the sum of all arguments.
function sum(...nums) {
  // TODO
  return 0;
}

// compose returns a function that computes f(g(x)).
function compose(f, g) {
  // TODO
  return (x) => x;
}

module.exports = { multiply, sum, compose };
