#include <stdio.h>

/* max returns the larger of a and b. */
int max(int a, int b) {
    /* TODO */
    return 0;
}

/* factorial returns n! for n >= 0. */
int factorial(int n) {
    /* TODO */
    return 0;
}

/* is_palindrome returns 1 if n reads the same forwards and backwards, else 0. */
int is_palindrome(int n) {
    /* TODO */
    return 0;
}

int main(void) {
    printf("max(3, 7) = %d\n", max(3, 7));
    printf("factorial(5) = %d\n", factorial(5));
    printf("is_palindrome(121) = %d\n", is_palindrome(121));
    printf("is_palindrome(123) = %d\n", is_palindrome(123));
    return 0;
}
